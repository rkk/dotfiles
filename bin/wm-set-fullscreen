#!/bin/bash
#
# Sets the focused window in fullscreen-mode.

wm-is-bspwm && bspc desktop -l next && exit 0
wm-is-i3 && i3-msg "fullscreen" && exit 0

echo "ERROR: Unsupported window manager" && exit 1

