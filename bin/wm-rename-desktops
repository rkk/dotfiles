#!/bin/bash
#
# Renames the desktops.


wm-is-i3 && echo "ERROR: Window manager not supported" && exit 1
desktops="$(bspc query -D)"
i=1
for k in ${desktops}
do
    bspc desktop "${k}" -n ${i}
    i=$((i+1))
done

