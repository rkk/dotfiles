# Simple X Hotkey Daemon (sxhkd) configuration for an almost window manager
# agnostic experience. Depends on xdotool, custom scripts and is aimed at
# a specific collection of applications, see "windowactivate" parts.

super + Return
    xterm

control + space
    dmenu_run \
    	-nb '#ffffea' \
    	-nf '#000000' \
    	-sb '#eaffff' \
    	-sf '#121212' \
    	-fn 'GoRegular' \
    	-l 10 \
    	-f \
    	-i

super + shift + space
    rofi -show window

super + F1
    amixer set Master toggle

super + F2
    amixer set Master '5%-'

super + F3
    amixer set Master '5%+'

super + F8
    brightnessctl set 10%-

super + F9
    brightnessctl set +10%

super + q
    wm-kill-window

super + w
    wm-close-window

super + space ; {Left,Right,Up,Down}
    wm-resize-window {west,east,north,south}

super + shift + {h,j,k,l}
    wm-move-direction {left,up,down,right}

super + Escape
    pkill -USR1 -x sxhkd

super + shift + Escape
    wm-reload

control + shift + d
    xdotool click 2

control + shift + r
	xdotool click 3

@F11
    wm-set-fullscreen

super + g
    wm-swap-size

super + {h,j,k,l}
    wm-focus-direction {left,down,up,right}

super + space ; {1,2,3,4}
    wm-move-desktop {1,2,3,4}

super + space ; g
   wm-set-floating

super + space ; {h,v}
   wm-split-direction {horizontal,vertical}


#
# PROGRAMMING ORIENTED KEYBOARD ENVIRONMENT (POKE)
#

control + shift + t
    xdotool search --classname xterm windowactivate

control + shift + z
    xdotool search  --classname Zathura windowactivate

control + shift + g
    wm-focus-name Thunderbird

control + shift + c
	wm-focus-name Gvim

control + shift + f
    wm-focus-name Chromium

control + shift + h
    wm-focus-name Slack

control + shift + m
    wm-focus-name Firefox

control + shift + s
    wm-focus-name Teams

#
# CUSTOM CONFIG: BSPWM
#

super + {Left,Right}
    bspc desktop -f {prev,next}

super + shift + {Left,Right}
    bspc node -d {prev,next} --follow

super + space ; {r,R}
    bspc node @/ --rotate {90,-90}


